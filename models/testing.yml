models:
  - name: customerrevenue
    description: "{{doc('list_custom')}}"
    columns:
      - name: PC_ORDER
        tests:
          - number_is_positive
      - name: NB_ORDER
        tests:
          - number_is_positive
  - name: nborders
    description: the number of orders!!
    columns:
      - name: NB_ORDERS
        tests:
          - number_is_positive
